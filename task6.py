s = "apple,banana,cherry,dates"

# Split into a list using the delimiter ','
substrings = s.split(',')

# Join the list of substrings with spaces
joined_string = ' '.join(substrings)

print("List of substrings:", substrings)
print("Joined string with spaces:", joined_string)